# =============================================================================
# API Test Configuration - Default Environment
# =============================================================================
# Override via:
#   System property: -Dapi.base.url=https://example.com
#   Environment var: API_BASE_URL=https://example.com
#   Different env:   mvn test -Denv=staging
# =============================================================================

# API Base URL
api.base.url=https://restful-booker.herokuapp.com

# Request Timeout (seconds)
api.timeout=30

# Authentication Credentials
api.auth.username=admin
api.auth.password=password123

# Logging Configuration
api.log.requests=true
api.log.responses=true
